# The classic set: syntax, undefined names, unused imports. The broad default of ruff >= 0.16
# would add style rules this tree does not hold its scripts to.
line-length = 100
target-version = "py314"

[lint]
select = ["E4", "E7", "E9", "F"]
